experiment_name: real_small_training

base:
  config: configs/train_config.yaml
  mock: false
  teacher_type: hf
  student_type: mamba
  teacher_model_name_or_path: sshleifer/tiny-gpt2
  tokenizer_name_or_path: sshleifer/tiny-gpt2
  local_files_only: true
  dataset_type: text
  data_path: ${CSDM_DATA_PATH}
  seq_len: 128
  batch_size: 1
  gradient_accumulation_steps: 16
  max_steps: 1000
  student_hidden_size: 128
  student_num_layers: 4
  mixed_precision: bf16
  top_k: 128
  teacher_cache_enabled: true
  save_every_steps: 100
  save_at_end: true

variants:
  - name: kd
    requires_mamba: true
    ce_weight: 0.2
    kd_weight: 1.0
    csdm_weight: 0.0
    topk_enabled: false
    teacher_cache_dir: /tmp/csdm_real_small/cache/kd
    checkpoint_output_dir: /tmp/csdm_real_small/checkpoints/kd

  - name: csdm
    requires_mamba: true
    ce_weight: 0.2
    kd_weight: 1.0
    csdm_weight: 0.03
    topk_enabled: false
    teacher_cache_dir: /tmp/csdm_real_small/cache/csdm
    checkpoint_output_dir: /tmp/csdm_real_small/checkpoints/csdm

  - name: csdm_topk
    requires_mamba: true
    ce_weight: 0.2
    kd_weight: 1.0
    csdm_weight: 0.03
    topk_enabled: true
    top_k: 128
    teacher_cache_dir: /tmp/csdm_real_small/cache/csdm_topk
    checkpoint_output_dir: /tmp/csdm_real_small/checkpoints/csdm_topk
